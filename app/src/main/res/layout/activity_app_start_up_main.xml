<?xml version="1.0" encoding="utf-8"?>
<startup.app.speedopt.MainRootView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_app_start_up_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="startup.app.speedopt.AppStartUpMainActivity">

    <startup.app.speedopt.FirstDrawLayoutRoot
        android:id="@+id/root_layout_one"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:background="@color/colorAccent">

        <startup.app.speedopt.FirstDrawLayoutRoot
            android:id="@+id/root_layout_one_child"
            android:layout_width="match_parent"
            android:layout_height="30dp"
            android:layout_gravity="center_vertical"
            android:background="@color/colorPrimary" />

    </startup.app.speedopt.FirstDrawLayoutRoot>

    <startup.app.speedopt.FirstDrawLayoutRoot
        android:id="@+id/root_layout_two"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_marginTop="80dp"
        android:background="@color/colorAccent">

        <startup.app.speedopt.FirstDrawLayoutRoot
            android:id="@+id/root_layout_two_child"
            android:layout_width="match_parent"
            android:layout_height="30dp"
            android:layout_gravity="center_vertical"
            android:background="@color/colorPrimary" />
    </startup.app.speedopt.FirstDrawLayoutRoot>

    <ListView
        android:id="@+id/time_log_listview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>
</startup.app.speedopt.MainRootView>
